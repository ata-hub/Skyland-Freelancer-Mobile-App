<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp">
    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/enterTaskTitle"
        android:hint="Enter the task title "
        android:textSize="20sp"
        android:layout_marginBottom="20dp"
        android:layout_marginTop="20dp"/>
    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter due date of task with dashes (yyyy-mm-dd)"
        android:textSize="20sp"
        android:id="@+id/enterTaskDay"
        android:layout_below="@id/enterTaskTitle"
        android:layout_marginBottom="20dp"/>


    <EditText
        android:id="@+id/enterTaskDescription"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/enterTaskDay"
        android:gravity="top"
        android:hint="Enter task description "
        android:inputType="textMultiLine"
        android:lines="5"
        android:textSize="20sp" />

</RelativeLayout>